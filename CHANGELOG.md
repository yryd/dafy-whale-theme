# Changelog

## 2.0.0 — 2026-09-20

- **新增「设置 → 海洋主题」面板**：品牌区 / 配色 / 背景与氛围 / 鱼群 / 每日鱼语 / 界面，共 32 项可调，改动即时生效并持久化。
- 鱼群的数量、速度、透明度、大小从写死改为可调；气泡数量与速度同。
- 主色可调，整套色阶自动派生；错误/成功/警告状态色保持不变。
- 新增仿官方构建版本徽章（默认隐藏，文字可自定义）。
- 新增整体缩放（同时作用于品牌标记、鱼群、气泡、背景水印）。
- **构建改造**：引入 TypeScript + 纯 `tsc`（无打包器），产物 `lib/index.js`（宿主 ESM）与 `lib/client.cjs`（客户端 ModuleLoader）；新增 DSH 契约校验（16 项）、schema 对齐校验（5 项），以及覆盖配置模型、客户端接线与宿主端的 `node:test` 测试。
- 运行时依赖仅 `@deepseek-ai/schemastery`（DSH 设置 schema 的要求）。
- ⚠️ 破坏性变更：入口从 `index.mjs` / `client.js` 变为 `lib/`；从 git 直接安装不再可用，请用 npm 安装。

## 1.2.1 — 2026-09-20

- **修复**：左上角「蓝色大肥鱼」重复显示 4 次。
- 品牌替换改用官方 `sidebar.brand.mark` / `sidebar.brand.name` 插槽，不再依赖 DSH 内部 CSS 类名。
- 修正顶部过时素材注释。

## 1.2.1-rc.0

- 发布前幂等守卫，避免重推 tag 时报错。

## 1.2.0

- 删除吉祥物，清除素材许可债务（tarball 4.6MB → 1.4MB）。
- 上架 npm，OIDC 自动发布。
